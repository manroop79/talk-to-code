apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: tai-gps
  name: tai-frontend-trustworthy-%%VERSION%%
  labels:
    app: tai-frontend-trustworthy-%%VERSION%%
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tai-frontend-trustworthy-%%VERSION%%
  template:
    metadata:
      labels:
        app: tai-frontend-trustworthy-%%VERSION%%
    spec:
      automountServiceAccountToken: true
      securityContext:
        runAsUser: 0
        runAsGroup: 0
      containers:
        - name: tai-frontend-trustworthy
          image: 225665832482.dkr.ecr.us-east-1.amazonaws.com/fema/trustworthy:%%TAG%%
          imagePullPolicy: Always
          resources:
            requests:
              memory: "10Gi"
              cpu: "1"
              nvidia.com/gpu: 0
            limits:
              memory: "20Gi"
              cpu: "4"
              nvidia.com/gpu: 0
      nodeSelector:
        app: llm-guard-ng
      tolerations:
          - key: llm-guard-ng
            operator: Equal
            value: "true"
            effect: NoSchedule
---
apiVersion: v1
kind: Service
metadata:
    labels:
      app: tai-frontend-trustworthy-%%VERSION%%
    name: tai-frontend-trustworthy-%%VERSION%%
    namespace: tai-gps
spec:
    ports:
    - port: 80
      protocol: TCP
      targetPort: 80
    selector:
      app: tai-frontend-trustworthy-%%VERSION%%
    type: LoadBalancer
